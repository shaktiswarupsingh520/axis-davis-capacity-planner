type SnapshotMetric = { label: string; value: number };
const numericFromText = (value: string) => { const match = value.replace(/,/g, '').match(/-?\d+(?:\.\d+)?/); return match ? Number(match[0]) : 0; };
function enhanceSimulation() { const panel = document.querySelector<HTMLElement>('.simulation-enhanced'); if (!panel) return; const cards = [...panel.querySelectorAll<HTMLElement>('.metric-card')]; const values: SnapshotMetric[] = cards.slice(0, 3).map((card) => ({ label: card.querySelector('span')?.textContent?.trim() || 'Traffic', value: numericFromText(card.querySelector('strong')?.textContent || card.textContent || '0') })); const current = values.find((item) => item.label.toLowerCase().includes('current'))?.value ?? 0; const simulated = values.find((item) => item.label.toLowerCase().includes('simulated'))?.value ?? 0; const signature = `${current}|${simulated}`; const existing = panel.querySelector<HTMLElement>('.simulation-scenario-chart'); if (existing?.dataset.signature === signature) return; existing?.remove(); const max = Math.max(1, current, simulated); const currentY = 150 - (current / max) * 110; const simulatedY = 150 - (simulated / max) * 110; const chart = document.createElement('section'); chart.className = 'simulation-scenario-chart chart-wrap'; chart.dataset.signature = signature; chart.innerHTML = `<div class="chart-title-row"><strong>Traffic scenario projection</strong><span>Unit: req/min · X-axis: Simulation horizon</span></div><svg viewBox="0 0 820 250" role="img" aria-label="Current traffic compared with simulated traffic"><line x1="76" x2="780" y1="150" y2="150" class="chart-grid"/><line x1="76" x2="780" y1="95" y2="95" class="chart-grid"/><line x1="76" x2="780" y1="40" y2="40" class="chart-grid"/><polyline points="76,${currentY} 252,${currentY} 428,${currentY} 604,${currentY} 780,${currentY}" class="scenario-line actual"/><polyline points="76,${currentY} 252,${currentY + (simulatedY-currentY)*.25} 428,${currentY + (simulatedY-currentY)*.5} 604,${currentY + (simulatedY-currentY)*.75} 780,${simulatedY}" class="scenario-line forecast"/><circle cx="780" cy="${currentY}" r="4" class="scenario-dot actual"/><circle cx="780" cy="${simulatedY}" r="4" class="scenario-dot forecast"/><text x="62" y="154" class="chart-axis-label">0</text><text x="32" y="99" class="chart-axis-label">${Math.round(max*.5)}</text><text x="32" y="44" class="chart-axis-label">${Math.round(max)}</text><text x="70" y="214" class="chart-axis-label">Now</text><text x="350" y="214" class="chart-axis-label">Scenario midpoint</text><text x="724" y="214" class="chart-axis-label">Horizon</text><text x="345" y="238" class="chart-axis-title">Simulation horizon</text></svg><div class="chart-legend"><span><i class="legend-dot actual-dot"/>Current traffic</span><span><i class="legend-dot forecast-dot"/>Simulated traffic</span></div><div class="chart-insights"><div><small>Current baseline</small><strong>${current.toFixed(1)} req/min</strong></div><div><small>Simulated endpoint</small><strong>${simulated.toFixed(1)} req/min</strong></div><div><small>Traffic delta</small><strong>+${Math.max(0, simulated-current).toFixed(1)} req/min</strong></div><div><small>Scenario assumption</small><strong>Linear ramp</strong></div></div>`; const result = panel.querySelector('.simulation-result'); result?.after(chart) ?? panel.appendChild(chart); }
function enhanceForecast() { const panel = document.querySelector<HTMLElement>('.forecast-panel'); if (!panel) return; const chart = panel.querySelector('.chart-wrap'); if (!chart) return; const existing = panel.querySelector<HTMLElement>('.forecast-interpretation'); const insights = [...chart.querySelectorAll<HTMLElement>('.chart-insights > div')].map((node) => ({ label: node.querySelector('small')?.textContent?.trim() || '', value: node.querySelector('strong')?.textContent?.trim() || '' })); const hasForecast = Boolean(panel.querySelector('.chart-line.forecast')); const html = `<div class="forecast-interpretation-title">How to read this forecast</div><div class="forecast-interpretation-grid"><div><small>Forecast status</small><strong>${hasForecast ? 'Forecast plotted' : 'No usable forecast points'}</strong></div>${insights.map((item) => `<div><small>${item.label}</small><strong>${item.value}</strong></div>`).join('')}</div><p>${hasForecast ? 'Blue = observed telemetry. Orange = Dynatrace forecast. Gold band = 90% prediction interval. Red line = 80% capacity threshold. Read the peak and the prediction band together; a high band near or above the threshold indicates uncertainty that may warrant capacity action.' : 'Dynatrace Intelligence returned no usable forecast line for this run. The application intentionally does not fabricate a trend.'}</p>`; if (!existing) { const node = document.createElement('section'); node.className = 'forecast-interpretation'; node.innerHTML = html; panel.appendChild(node); } else existing.innerHTML = html; }
export function installUiEnhancements() { const state = window as Window & { __axisEnhancementsInstalled?: boolean }; if (state.__axisEnhancementsInstalled) return; state.__axisEnhancementsInstalled = true; const refresh = () => { const active = document.querySelector('.nav-item.active')?.textContent?.trim() || ''; if (active.includes('Simulation')) enhanceSimulation(); if (active.includes('Capacity Forecast')) enhanceForecast(); }; const observer = new MutationObserver(refresh); observer.observe(document.body, { subtree: true, childList: true, characterData: true, attributes: true }); window.setInterval(refresh, 600); window.setTimeout(refresh, 800); }
